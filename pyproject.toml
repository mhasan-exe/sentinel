[project]
name = "example-database"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "mhasan-exe", email = "mikailfardan221@gmail,com" }
]
requires-python = ">=3.14"
dependencies = [
    "fastapi>=0.141.1",
    "sqlmodel>=0.0.39",
    "cryptography>=42.0.0",
    "pqcrypto>=0.3.4",
]

[project.scripts]
example-database = "example_database:main"

[build-system]
requires = ["uv_build>=0.12.3,<0.13.0"]
build-backend = "uv_build"
